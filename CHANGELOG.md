# Changelog

All notable changes to this repository are documented here.

## [Unreleased]

### Added

- **ClearlyMD** — Editor workflow under **`ClearlyMD/`**: setup guide, `install-clearlymd.sh`, `setup-clearlymd.sh`, `clearlyedit-new-md.sh`, `build-clearlyedit-app.sh`, `ClearlyEdit.app`. Editor zip is distributed via **this repo’s** GitHub Releases (tag `clearlymd-latest`, asset `Clearly-Debug-unsigned.zip`).

### Previous

- Repository skeleton (`README.md`, `CHANGELOG.md`, `.gitignore`).
- **TextMD** — TextEdit notes workflow bundle (see `TextMD/TextMD-SetupGuide.md`).
