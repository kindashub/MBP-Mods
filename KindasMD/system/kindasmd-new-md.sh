#!/usr/bin/env bash
# Deprecated: use **kindasmd** (same directory). Kept so old notes/scripts that reference this filename still work.
exec "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/kindasmd" "$@"
